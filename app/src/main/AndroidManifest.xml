<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Navigate3"
            android:label="@string/title_activity_navigate3"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Navigate2"
            android:label="@string/title_activity_navigate2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".navigate"
            android:label="@string/title_activity_navigate"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".page2" />
        <activity android:name=".animal" />
        <activity android:name=".human" />
        <activity android:name=".environment" />
        <activity android:name=".sign" />
        <activity android:name=".signupmain" />
        <activity android:name=".loginmain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>